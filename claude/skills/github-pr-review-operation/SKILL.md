---
name: github-pr-review-operation
description: PRレビュー操作。PRレビュー、コードレビュー、PR操作が必要な時に使用。
---

# 実行方法

PRレビュー操作の詳細なロジックは `/github-pr-review-operation` コマンドに定義されています。

**このスキルが選択された場合、必ず `/github-pr-review-operation` コマンドを実行してください。**

直接 `gh` コマンドを実行せず、コマンド経由で操作を行うこと。
